package PracticeFolder;

import org.openqa.selenium.By;
import org.openqa.selenium.JavascriptExecutor;
import org.openqa.selenium.WebElement;

public class JSEExample {
    public static void main(String[] args) {
       // define javascripexecutor
    //    JavascriptExecutor scroll = (JavascriptExecutor) driver;
        //scroll down 500 pixels
     //   scroll.executeScript("Scroll(0,500");
        //scrolling up 500 pixels
      //  scroll.executeScript("Scroll(0,-500)");
        //scrolling right(+) 500 pixels
        //     scroll.executeScript("Scroll(500,0)");
        //scrolling left(-) 500 pixels
     //   scroll.executeScript("Scroll(-500,0)");

        //define web element
    //    WebElement calculateButton = driver.findElement(By.xpath("//*[@class='calculator']"));
        //
    //    scroll.executeScript("argument[0].scrollIntoView(true);",calculateButton);
    }//end of main
}//end of class
